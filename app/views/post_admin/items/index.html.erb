<h1>商品投稿一覧画面</h1>
  <% @items.each do |item| %>
    <%= link_to post_admin_item_path(item.id) do %>
      <%= item.name %></br>
      <%= image_tag item.image %></br>
      <%= item.introduction %></br>
      <% end %>
  <% end %>